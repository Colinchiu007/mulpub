# Multi-Publish 产品使用说明书

> **版本**：v2.3.41 | **最后更新**：2026-07-08
> **产品定位**：多平台内容一键发布桌面工具
> **目标用户**：自媒体运营者、MCN 机构、企业内容团队
> **技术栈**：Electron 33 + Vue 3 + Python FastAPI + RpaViewManager RPA

---

## 目录

- [一、产品概述](#一产品概述)
- [二、快速开始](#二快速开始)
- [三、界面导览](#三界面导览)
- [四、账号管理](#四账号管理)
- [五、内容发布](#五内容发布)
- [六、内容采集](#六内容采集)
- [十三、内容创作](#十三内容创作)
- [十四、AI 辅助功能](#十四ai-辅助功能)
- [十五、发布日历](#十五发布日历)
- [十六、内容模板](#十六内容模板)
- [十七、发布任务管理](#十七发布任务管理)
- [十八、分屏监控](#十八分屏监控)
- [十九、评论管理](#十九评论管理)
- [二十、发布历史与统计](#二十发布历史与统计)
- [二十一、内容情报引擎](#二十一内容情报引擎)
- [二十二、爆款分析](#二十二爆款分析)
- [二十三、云端发布](#二十三云端发布)
- [二十四、内容质量检测](#二十四内容质量检测)
- [二十五、数据同步](#二十五数据同步)
- [二十六、插件系统](#二十六插件系统)
- [二十七、API 开放平台](#二十七-api-开放平台)
- [二十八、许可证与定价](#二十八许可证与定价)
- [二十九、系统功能](#二十九系统功能)
- [三十、常见问题](#三十常见问题)
- [三十一、更新日志](#三十一更新日志)

---

## 一、产品概述

### 1.1 什么是 Multi-Publish

Multi-Publish（社媒管家）是一款面向内容生产者的桌面端工具，提供 **"采集 → 改写 → 发布"** 全流程闭环的一键发布体验。

> ![产品主界面概览](images/main-interface.png)
> *图 1：Multi-Publish 主界面 — 左侧导航栏 + 右侧内容区*

**核心工作流：**

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  内容创作  │ → │ 内容采集  │ → │ 内容编辑  │ → │ 一键发布  │
│ (外部工具) │   │ URL/粘贴  │   │ 模板/优化  │   │ 多平台分发 │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                    │
                                              ┌─────┴─────┐
                                              │ 任务队列管理 │
                                              │ 实时进度追踪 │
                                              └───────────┘
```

### 1.2 核心价值

| 能力 | 说明 |
|------|------|
| **统一入口** | 一个桌面应用管理 15 个平台的发布 |
| **自动适配** | RPA 自动化填表发布，适配各平台 UI |
| **异步队列** | 后台批量发布，实时追踪状态 |
| **多账号管理** | 同平台可添加多个账号，一键切换 |
| **定时发布** | 设定时间自动发布，关机后重启恢复 |
| **安全存储** | Cookie AES-256-GCM 加密存储 |
| **离线可用** | 安装包自带 Chromium，无需额外下载 |

### 1.3 支持平台（15 个）

| 平台 | 内容类型 | 技术路线 | 多账号 | 分类 |
|------|----------|----------|:------:|:----:|
| 微信公众号 | 图文 | RPA | ✅ | 图文 |
| 知乎 | 图文 | RPA | ✅ | 图文 |
| 微博 | 图文 | RPA | ✅ | 图文 |
| 小红书 | 图文 | RPA | ✅ | 图文 |
| 百家号 | 图文 | RPA | ✅ | 图文 |
| 今日头条 | 图文 + 视频 | RPA | ✅ | 混合 |
| 抖音 | 图文 + 视频 | RPA | ✅ | 视频 |
| 视频号 | 视频 + 图文 | RPA | ✅ | 视频 |
| 快手 | 视频 + 图文 | RPA | ✅ | 视频 |
| B站 | 专栏 + 视频 | API + RPA 双模式 | ✅ | 视频 |
| YouTube | 视频 | RPA + OAuth 2.0 | ✅ | 视频 |
| TikTok | 视频 | RPA + OAuth 2.0 | ✅ | 视频 |
| Instagram | 图片 + 视频 + Reels | RPA | ✅ | 混合 |
| Facebook | 图文 + 视频 + 链接 | RPA | ✅ | 混合 |
| Twitter/X | 图文 | RPA | ✅ | 图文 |

### 1.4 技术架构

```
┌──────────────────────────────────────────────────────────┐
│                    Multi-Publish Desktop App              │
│  ┌─────────────────────┐  ┌────────────────────────────┐ │
│  │  Vue 3 前端 (7 页面)  │  │  Electron 主进程 (35+ 模块) │ │
│  │  ┌─────────────────┐ │  │  ┌──────────────────────┐ │ │
│  │  │ 设计体系 (奶油·   │ │  │  │ IPC Bridge           │ │ │
│  │  │ 薰衣草)          │ │  │  │ preload.js           │ │ │
│  │  │ UiButton/UiCard  │ │  │  └──────────────────────┘ │ │
│  │  │ UiModal/UiSelect │ │  │  ┌──────────────────────┐ │ │
│  │  │ PlatformIcon 等  │ │  │  │ 模块层               │ │ │
│  │  └─────────────────┘ │  │  │ TaskQueue / Scheduler │ │ │
│  │  Pinia 状态管理       │  │  │ Store / OAuth        │ │ │
│  │  Vue Router 路由      │  │  │ QrCode / HotKeys     │ │ │
│  └─────────────────────┘  │  │ SystemTray / Updater  │ │ │
│                           │  └──────────────────────┘ │ │
│                           └────────────────────────────┘ │
│                                    │                     │
│  ┌────────────────────────────────────────────────┐     │
│  │  RPA 引擎层                                     │     │
│  │  ┌──────────────────────────────────────────┐  │     │
│  │  │  RpaViewManager                          │  │     │
│  │  │  隐藏 BrowserWindow + executeJavaScript  │  │     │
│  │  │  + CDP 文件上传 + 网络监听 + Session 隔离  │  │     │
│  │  └──────────────────────────────────────────┘  │     │
│  │  ┌──────────┐ ┌──────────┐ ┌────────────────┐ │     │
│  │  │ 15 平台   │ │ 扫码登录  │ │ CallbackServer │ │     │
│  │  │ 发布器    │ │ 3 策略    │ │ :16521         │ │     │
│  │  └──────────┘ └──────────┘ └────────────────┘ │     │
│  └────────────────────────────────────────────────┘     │
│                           │                             │
│  ┌──────────────┐  ┌─────────────────────────────┐     │
│  │  SQLite 存储  │  │  Python FastAPI 后端          │     │
│  │  6 张数据表   │  │  (API 模式 / 云端发布)         │     │
│  └──────────────┘  └─────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘
```

### 1.5 设计语言

> ![设计预览](images/design-preview.png)
> *图 2：奶油·薰衣草设计体系预览*

| 设计元素 | 规范 |
|---------|------|
| **灵魂色** | 浅薰衣草紫 `#7c5cbf` |
| **基底色** | 奶油白 `#ffffff` / 浅紫底 `#f8f4ff` |
| **标题字体** | Satoshi (英文) + Noto Serif SC 思源宋体 (中文) |
| **正文字体** | DM Sans (英文) + 阿里巴巴普惠体 (中文) |
| **等宽字体** | JetBrains Mono |
| **圆角风格** | 8px~20px 柔和圆角 |
| **动效原则** | 克制而有目的（hover 150ms / 过渡 300ms） |
| **装饰细节** | 纹理叠加 0.02 透明度、柔和阴影、平滑悬浮过渡 |

---

## 二、快速开始

### 2.1 系统要求

| 项目 | 最低要求 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Windows 10 / macOS 12+ / Linux | Windows 11 / macOS 14+ |
| 内存 | 4GB | 8GB+ |
| 磁盘空间 | 1GB（含内置 Chromium ~170MB） | 2GB |
| 网络 | 需要联网登录 | 宽带连接 |
| 显示器 | 1280×720 | 1920×1080+ |

### 2.2 下载安装

**方式一：GitHub Releases（推荐）**

1. 访问 [GitHub Releases](https://github.com/Colinchiu007/Multi-Publish/releases)
2. 根据系统下载对应安装包：

| 系统 | 安装包 | 说明 |
|------|--------|------|
| Windows | `Multi-Publish.Setup.x.x.x.exe` | 双击安装 |
| macOS | `Multi-Publish-x.x.x.dmg` | 拖入 Applications |
| Linux | `Multi-Publish-x.x.x.AppImage` | `chmod +x` 后运行 |

**方式二：Gitee 国内镜像**

国内用户推荐从 [Gitee Releases](https://gitee.com/ColinChiu007/Multi-Publish/releases) 下载，330MB 文件通常 1-2 分钟完成。

**方式三：源码构建**

```bash
git clone https://github.com/Colinchiu007/Multi-Publish.git
cd Multi-Publish
npm install
npm run dev    # 开发模式
npm run build:win  # 构建 Windows 安装包
```

### 2.3 首次启动

> ![首次启动引导](images/first-run.png)
> *图 3：首次启动引导 Wizard — 4 步完成初始设置*

首次启动时，App 自动完成：

```
启动 App → 检测 Python 环境 → 安装依赖 → 启动后端 → 打开引导 Wizard
    (1-2 分钟)         (自动)        (自动)       (自动)      (4 步设置)
```

**Onboarding Wizard（4 步引导）：**

| 步骤 | 操作 | 说明 |
|:----:|------|------|
| ① | **平台选择** | 勾选你常用的发布平台 |
| ② | **账号绑定** | 为每个平台添加账号（扫码/登录） |
| ③ | **内容设置** | 配置默认发布设置 |
| ④ | **完成** | 进入主界面 |

> 你也可以随时点击「跳过引导」，通过左侧导航栏手动配置。

### 2.4 主界面布局

> ![主界面布局标注](images/main-interface-annotated.png)
> *图 4：主界面布局 — 左侧导航栏 + 中间内容区 + 右侧工具区*

```
┌──────────┬──────────────────────────────────────┐
│ 导航栏    │              内容区域                   │
│ 240px    │                                        │
│          │  ┌──────────────────────────────────┐ │
│ 🏠 首页   │  │  页面标题                         │ │
│ 📝 发布   │  │                                  │ │
│ 👤 账号   │  │  [主要操作区]                     │ │
│ 📊 看板   │  │                                  │ │
│ 📡 监控   │  │                                  │ │
│ 📋 采集   │  │                                  │ │
│ 🧠 情报   │  └──────────────────────────────────┘ │
│ 🔌 插件   │                                        │
│ ⚙️ 设置   │          ┌────────┬────────┐          │
│          │          │ 状态栏  │ 托盘   │          │
│          │          │ 3任务   │ 通知   │          │
│          │          └────────┴────────┘          │
└──────────┴──────────────────────────────────────┘
```

**左侧导航栏功能：**

| 图标 | 名称 | 说明 |
|:----:|------|------|
| 🏠 | 首页 | 概览仪表盘、快捷操作入口 |
| 📝 | 一键发布 | 内容编辑与发布 |
| 👤 | 账号管理 | 平台账号的增删改查 |
| 📊 | 统计看板 | 发布历史与数据分析 |
| 📡 | 分屏监控 | 多平台实时监控 |
| 📋 | 内容采集 | URL 采集与草稿箱 |
| 🧠 | 情报中心 | 内容情报与爆款分析 |
| 🔌 | 插件管理 | 插件安装与管理 |
| ⚙️ | 设置 | 应用设置与许可证 |

---

## 三、界面导览

### 3.1 首页

> ![首页仪表盘](images/home-dashboard.png)
> *图 5：首页仪表盘 — 快捷操作 + 发布概览*

首页分为三个区域：

| 区域 | 内容 |
|:----:|------|
| **顶部** | 欢迎语 + 快速发布入口 |
| **中部** | 最近发布状态卡片（今日发布数、成功率、待处理任务数） |
| **底部** | 快捷操作：创建新文章、前往账号管理、查看统计 |

### 3.2 命令面板

> ![命令面板](images/command-palette.png)
> *图 6：命令面板 — `Ctrl+K` 或 `⌘K` 快速调用*

按下 **`Ctrl+K`**（Windows/Linux）或 **`⌘K`**（macOS）打开命令面板，可以：

- 快速导航到任意页面
- 搜索功能名称
- 执行快捷操作（如"添加账号"、"查看发布队列"）
- 支持模糊搜索

### 3.3 发布页面

> ![发布页面布局](images/publish-page.png)
> *图 7：发布页面 — 左侧平台选择 + 中间编辑器 + 右侧设置*

| 区域 | 内容 |
|:----:|------|
| **左侧** | 平台列表（按分类分组：视频/图文/混合，显示已登录账号数量） |
| **中间** | 富文本编辑器（标题 + 正文，支持 Markdown/富文本切换） |
| **右侧** | 发布设置（定时开关、视频上传、封面设置、模板选择） |

### 3.4 设置页面

> ![设置页面](images/settings-page.png)
> *图 8：设置页面 — 语言切换、许可证管理、代理配置*

| 设置项 | 说明 |
|--------|------|
| **语言** | 中文 / English 切换 |
| **许可证** | 查看当前版本、激活 Pro、试用 |
| **代理设置** | HTTP/HTTPS/SOCKS 代理配置 |
| **发布设置** | 并发数、重试次数、发布间隔 |
| **关于** | 版本号、更新检测、开源信息 |

---

### 3.5 模型设置

模型设置页管理视频创作、文案优化等 AI 功能使用的模型服务商（推理模型 LLM / TTS 语音 / 语音识别 / 图片生成 / 视频模型 / 音频生成 / 多模态）。

**入口：** 设置 → 模型设置（或顶部模型设置入口）。

| 设置项 | 说明 |
|--------|------|
| **服务商列表** | 按类别分组展示预设与自定义服务商；可新增、编辑、禁用、删除（预设服务商不可删除，只能禁用） |
| **API Key** | 每个服务商独立保存（系统安全加密）；本地免 Key 服务商（Piper / Local Diffusion / ComfyUI）可留空 |
| **Base URL / 模型列表** | 自定义服务商可修改；启用运营后台同步后，预设服务商的模型列表为只读 |
| **限流（每分钟连接次数 / 5小时限额次数）** | **只读展示**：数值由运营后台同步下发，或使用服务商默认值；未配置时显示「未配置（默认限流）」 |
| **运营后台同步** | 填写运营后台地址 + 目录同步 API Key；支持「立即同步」与「启动时自动同步」；启用后服务商限流与模型列表以运营后台为准 |

**限流与排队行为说明：**

- 每次调用模型前，系统按该服务商的「每分钟连接次数」自动安排并发与排队：并发上限约为每分钟连接次数的 1/10（下限 1、上限 4）；视频/音频类未配置时并发保持 1。
- 超出并发上限或每分钟频率的请求会**自动排队等待**（并发队列最长约 30 秒，频率窗口最长约 3 分钟），而不是直接失败；排队超时给出明确提示「当前请求频率已达上限，请稍后再试。」。
- 服务商返回 429 限流响应后，系统自动进入冷却并带退避重试（最多 3 次），并临时下调该服务商每分钟预算（收到 429 下调 25%，成功后逐步恢复）。
- 配置了「5小时限额次数」的服务商，每 5 小时请求次数达到上限后将提示额度已用完，需检查套餐额度或更换模型；系统不会自动重试额度类失败。
- 未配置限流的服务商使用内置服务商默认值（保守估计），不会因缺少配置而报错或阻塞。
- 运营后台清空限流值时，桌面端回退到服务商默认限流，行为与未配置一致。

#### 3.6 运营后台同步

> 适用于已部署 Multi-Publish 运营后台（ops-center）的团队；个人本地使用无需配置。

在【模型设置】页顶部「运营后台同步」卡片填写运营后台地址与目录同步 API Key：

| 操作 | 说明 |
|------|------|
| 填写地址 | 必须为 http(s) URL（非本机地址强制 https） |
| 填写 API Key | 与运营后台 `OPS_CATALOG_API_KEY` 一致；加密保存，不显示明文 |
| 立即同步 | 手动拉取一次运营配置（服务商限流 / 模型 / 能力 / 默认模型） |
| 启动时自动同步 | 勾选后每次启动桌面端自动同步（启动后约 3 秒） |
| 启用提示 | 「已启用运营后台下发：服务商的『每分钟连接次数 / 5小时限额次数 / 模型列表』以运营后台为准，桌面端为只读展示；本地仍可配置 API Key、Base URL 与默认服务商。」 |

常见状态提示：`尚未同步`、`上次同步：{{时间}}`、`同步中...`、`同步成功：更新 N 个服务商`、`API Key 无效`、`未启用目录同步`、`同步请求超时（10 秒）`、`无法连接 Ops Center`。

---

## 四、账号管理

### 4.1 添加账号

> ![账号管理页面](images/accounts-page.png)
> *图 9：账号管理页面 — 按平台分组展示所有账号*

**操作步骤：**

1. 左侧导航栏点击 **「👤 账号管理」**
2. 点击右上角 **「+ 新增账号」**
3. 在弹出的对话框中选择平台（如"微信公众号"）
4. 系统根据平台类型选择登录方式：
   - **内嵌浏览器登录**：直接在应用内弹出登录框
   - **扫码登录**：显示二维码，用手机扫码
   - **OAuth 2.0 授权**：跳转到平台授权页面
5. 登录成功后自动关闭窗口，账号出现在列表中 ✅

> ![添加账号对话框](images/add-account-dialog.png)
> *图 10：添加账号对话框 — 选择平台 + 登录方式*

**各平台登录方式速查：**

| 平台 | 默认登录方式 | 备选方式 |
|------|------------|---------|
| 微信公众号 | 微信扫码 | — |
| 知乎 | 账号密码 | 手机号验证码 |
| 微博 | 扫码 | 账号密码 |
| 抖音 | 扫码 / 手机号 | 账号密码 |
| 小红书 | 扫码 | 账号密码 |
| 视频号 | 微信扫码 | — |
| 快手 | 手机号 | 账号密码 |
| 今日头条 | 扫码 | 账号密码 |
| 百家号 | 扫码 | 账号密码 |
| B站 | 扫码 | 账号密码 |
| YouTube | Google 账号授权 (OAuth 2.0) | — |
| TikTok | OAuth 2.0 授权 | 扫码 |
| Twitter/X | OAuth 授权 | 账号密码 |
| Instagram | 账号密码 | — |
| Facebook | 账号密码 | — |

### 4.2 多账号管理

**同平台多账号：**

Multi-Publish 支持同一平台添加多个账号，适合运营多个账号的团队。

> ![多账号切换](images/multi-account-switch.png)
> *图 11：侧栏多账号切换 — 点击账号头像快速切换*

**操作方式：**

| 操作 | 方法 |
|------|------|
| 添加多个账号 | 在同一平台多次点击"+ 新增账号" |
| 切换账号 | 侧栏点击平台下的账号头像 |
| 设置默认账号 | 账号管理页面点击星标 ★ |
| 编辑昵称 | 账号管理页面点击昵称编辑 |
| 删除账号 | 账号管理页面点击删除按钮 |

**状态指示灯：**

| 指示灯 | 含义 |
|:------:|------|
| 🟢 绿色 | Cookie 有效，登录态正常 |
| 🟡 黄色 | Cookie 即将过期（7天内） |
| 🔴 红色 | Cookie 已过期，需重新登录 |

### 4.3 登录状态管理

**自动检测机制：**

- App 启动时自动后台验证所有账号的 Cookie 有效性
- 使用隐藏 BrowserWindow 静默验证（loginSilent）
- 定期轮询检测登录状态

**过期处理：**

1. 账号过期时，状态指示灯变红
2. 发布时会自动检测并提示重新登录
3. 点击 **「重新登录」** 按钮 → 重新走登录流程
4. 新 Cookie 自动加密存储

**批量登录凭证自动保存与防丢失护栏（2026-09-22）：**

首页「登录失效提醒」横幅点击 **「批量登录」** 后，会为每个失效账号在独立浏览器标签
（session 分区 `persist:account-{id}`，以 cleanSession 干净身份打开登录页）中重新登录。
登录成功后凭证的持久化由三层机制保障，**不再依赖手动点击「保存账号」**：

1. **自动保存（治本）**：标签导航 URL 命中「登录成功判定」（处于平台 auth 域、非登录页、
   命中平台成功路径模式）后去抖 1500ms，主进程自动提取该标签分区的 Cookie + localStorage
   并回写加密凭证库。首帧加载完成前的平台自动重定向不算命中（防误判）；在登录页反复
   横跳时只在稳定后保存一次；保存失败保持「未保存」态并在下一次导航自动重试。
2. **关闭护栏（防丢失）**：若自动保存尚未完成，关闭未保存的账号标签时弹出确认框（三态）：
   - **保存并关闭**：先强制保存再关闭；即使保存失败也会提示「凭证未保存成功」并关闭，避免困住用户；
   - **直接关闭**：放弃本次登录，直接关闭标签；
   - **取消**（右上角 X）：什么都不做，留在页面。
3. **主动提醒（可发现性）**：
   - 未保存的账号标签标题右侧显示 **橙色未保存角标**（悬停/读屏提示「登录未保存」），保存成功后实时熄灭；
   - 当前活动账号标签未保存时，导航栏 **「保存账号」按钮呼吸脉冲动画** 提示点击（系统「减弱动态效果」时自动禁用）；
   - 横幅新增 **「全部保存（n）」** 按钮（仅存在未保存标签时显示，n 为数量），一键批量回写所有未保存账号标签，
     完成后按「成功 n 个 / 部分失败（列出原因）/ 全部失败」分类提示，失败账号不影响其余。

| 显示项 | 出现条件 | 消失条件 |
|--------|---------|---------|
| 橙色未保存角标（标签） | 账号标签以重新登录方式打开且凭证未回写 | 自动/手动/批量保存成功 |
| 「保存账号」按钮脉冲 | 当前活动账号标签未保存 | 同上 |
| 「全部保存（n）」按钮 | 未保存账号标签数 ≥ 1 | 计数归零 |

**数据校验与边界：** 查询/批量保存 IPC 均为纯 JSON 参数，tabId 缺失返回校验错误；
非账号标签或已保存标签直接放行关闭（保持原行为）；账号标签缺少 accountId/platform 时
保存 fail-closed（返回 not-account-tab）；提取到空凭证时由凭证库写入层抛错并保持未保存态；
关闭标签时自动清理未触发的去抖计时器，不会对已销毁视图执行保存。

### 4.4 三种认证模式

| 模式 | 实现方式 | 适用场景 |
|------|---------|---------|
| **内嵌浏览器登录** | WebContentsView 内嵌登录页面 | 大多数平台的标准登录 |
| **隐藏浏览器静默验证** | 隐藏 BrowserWindow 恢复 Cookie 检测 | 自动续期、后台检测 |
| **扫码登录** | QrCodeLogin 自动检测二维码（img/canvas 3 策略） | 微信生态平台 |

### 4.5 安全机制

| 安全措施 | 说明 |
|---------|------|
| **Cookie 加密** | AES-256-GCM 加密算法，密钥本地派生 |
| **凭证存储** | credential-store.js 统一管理，不落盘明文 |
| **Session 隔离** | `session.fromPartition()` 每个账号独立 Cookie 分区 |
| **不上传服务器** | 所有凭证仅存储在本地，不会上传到任何远程服务器 |

---

## 五、内容发布

### 5.1 图文发布（单篇）

> ![单篇发布流程](images/publish-flow.png)
> *图 12：单篇发布流程 — 选择平台 → 编辑内容 → 发布*

**操作步骤：**

| 步骤 | 操作 | 说明 |
|:----:|------|------|
| ① | 点击左侧 **「📝 一键发布」** | 进入发布页面 |
| ② | 在左侧平台列表中勾选目标平台 | 可多选，支持按分类筛选 |
| ③ | 输入**标题** | 各平台字符限制不同，标题优化助手可辅助 |
| ④ | 在编辑器中撰写内容 | 支持富文本 / Markdown 双模式 |
| ⑤ | （可选）上传封面 / 视频 | 右侧面板操作 |
| ⑥ | 选择发布账号 | 每个平台选择要使用的账号 |
| ⑦ | 点击 **「发布」** | 任务加入队列，实时显示进度 |

**富文本编辑器功能：**

> ![富文本编辑器](images/rich-editor.png)
> *图 13：ArticleEditor 组件 — Markdown/富文本切换、格式工具栏*

| 功能 | 说明 |
|------|------|
| **双模式切换** | 富文本模式 ↔ Markdown 模式一键切换 |
| **图片粘贴** | 直接 `Ctrl+V` 粘贴图片，自动上传 |
| **格式工具栏** | 加粗、斜体、标题、列表、引用、代码块 |
| **HTML 粘贴清洗** | 粘贴外部内容自动清洗格式，保留有效样式 |
| **字数统计** | 实时显示字数，适配各平台限制 |

### 5.2 视频发布

支持视频发布的平台：**抖音、视频号、快手、今日头条、B站、YouTube、TikTok、Instagram、Facebook**

> ![视频上传区域](images/video-upload.png)
> *图 14：视频上传区域 — 拖拽或点击选择文件*

**操作步骤：**

1. 在发布页面选择支持视频的平台
2. 右侧面板找到 **「视频上传」** 区域
3. 拖拽视频文件到上传区域，或点击选择文件
4. 等待上传完成（大文件走 CDP 通道，支持进度显示）
5. 填写标题和描述
6. 点击发布

**视频规格要求：**

| 项目 | 说明 |
|------|------|
| 支持格式 | `mp4`、`mov`、`avi` |
| 建议大小 | 不超过 500MB |
| 上传方式 | 大文件 → CDP `DOM.setFileInputFiles` / 小文件 → JS File API |
| 进度显示 | IPC `rpa:progress` 实时推送上传进度 |

### 5.3 批量发布

**多平台同时发布：**

> ![批量发布示意图](images/batch-publish.png)
> *图 15：批量发布 — 勾选多个平台，一次发布*

1. 在发布页面左侧勾选 **多个平台**
2. 编辑统一的内容
3. 点击发布 → 一条内容自动分发到所有选中平台
4. 任务队列依次执行各平台发布

**多账号同时发布：**

同平台有多个账号时，在账号选择下拉框中勾选多个账号：

> ![多账号同时发](images/multi-account-publish.png)
> *图 16：多账号同时发布 — 同平台选择多个账号*

- 选中多个账号 → 一条内容同时发送到所有选中账号
- 不同账号独立 Session 隔离，互不干扰

**批量编辑模式：**

适用于需要为不同平台**定制不同内容**的场景：

| 步骤 | 操作 |
|:----:|------|
| ① | 点击 **「批量编辑」** 按钮 |
| ② | 自动创建多篇文章卡片，每篇独立编辑 |
| ③ | 每篇文章独立选择目标平台和定时时间 |
| ④ | 批量复制：复制已有文章作为模板 |
| ⑤ | 点击「全部发布」或逐个发布 |

### 5.4 定时发布

> ![定时发布设置](images/scheduled-publish.png)
> *图 17：定时发布设置 — 选择日期和时间*

**操作步骤：**

1. 在发布页面右侧开启 **「定时发布」** 开关
2. 设置发布的 **日期** 和 **时间**（支持精确到分钟）
3. 点击发布 → 任务进入定时队列
4. 到设定时间自动触发发布

**定时任务特性：**

| 特性 | 说明 |
|------|------|
| **持久化** | 定时任务存储到 SQLite `scheduled_tasks` 表 |
| **崩溃恢复** | App 关闭后重启自动恢复未执行的定时任务 |
| **取消** | 在「发布历史」页面可取消等待中的定时任务 |
| **时间精度** | 基于 `node-cron` 调度，误差 < 1 秒 |

### 5.5 标题优化助手

> ![标题优化助手](images/title-assistant.png)
> *图 18：标题优化助手 — 自动生成优化建议*

在发布页面标题输入框旁点击 **「✨ 标题优化」** 按钮：

**支持 6 种标题模板：**

| 模板类型 | 示例 | 适用场景 |
|---------|------|---------|
| **数字型** | "5 个提升效率的方法" | 清单类、教程类 |
| **提问型** | "为什么你的内容没人看？" | 痛点类、分析类 |
| **教程型** | "如何从零开始做自媒体？" | 教程类、指南类 |
| **对比型** | "A vs B：哪个更适合你？" | 评测类、对比类 |
| **故事型** | "我做自媒体 3 年的真实经历" | 故事类、经验类 |
| **趋势型** | "2026 年内容创作最新趋势" | 行业分析、趋势类 |

**特性：**

- 基于搜索同类标题互动数据，自动评分推荐
- 自动适配各平台标题字符限制
- 点击建议标题可一键替换

### 5.6 封面生成器

> ![封面生成器](images/cover-generator.png)
> *图 19：封面生成器 — 从标题自动生成封面图*

自动从标题文字生成封面图，三种风格可选：

| 风格 | 效果 |
|:----:|------|
| **纯色风格 (Solid)** | 纯色背景 + 大号标题文字 |
| **渐变风格 (Gradient)** | 渐变背景 + 居中标题 |
| **深色风格 (Dark)** | 深色背景 + 浅色文字，适合严肃内容 |

**使用方式：**

1. 在发布页右侧面板找到「封面设置」
2. 点击「生成封面」
3. 选择风格
4. 预览满意后使用

### 5.7 格式适配

系统自动处理内容格式在各平台的适配：

| 格式 | 适配规则 |
|------|---------|
| **Markdown 标题** | `#` → 平台支持的标题格式 |
| **图片** | 自动上传到平台图床并插入 |
| **代码块** | → 平台支持的代码格式 |
| **列表** | → 平台兼容的列表样式 |
| **引用** | → 平台引用块格式 |

### 5.8 标题优化助手

> ![标题优化助手面板](images/title-assistant.png)
> *图 21：标题优化助手 — 同类标题互动数据分析*

**功能概述：**

标题优化助手通过分析跨平台同类标题的互动数据，为创作者提供标题优化建议。

**操作步骤：**

1. 在发布页编辑标题时，右侧自动显示标题优化面板
2. 系统实时分析输入的标题，给出优化建议
3. 展示同类标题高频词（带使用频率标记）
4. 展示高互动参考标题（来自 Reddit / HN / GitHub）

**高频词标记：**

- 🔴 **红色标签**：高频词（出现 ≥3 次）
- ⚪ **灰色标签**：中频词（出现 1-2 次）

---

## 六、内容采集

### 6.1 剪贴板导入

> ![剪贴板导入](images/clipboard-import.png)
> *图 20：从剪贴板导入内容*

**操作步骤：**

1. 在发布页面的编辑区，直接按 **`Ctrl+V`**（或 `⌘V`）
2. 系统自动解析剪贴板内容：
   - 提取标题（首行加粗文本）
   - 提取正文（保留富文本格式）
   - 提取图片（base64 自动上传）
3. 导入的内容可直接编辑后发布

### 6.2 URL 内容采集

> ![URL 采集页面](images/url-collection.png)
> *图 21：「内容采集」页面 — 输入链接自动提取*

**操作步骤：**

1. 左侧导航 → **「📋 内容采集」**
2. 在输入框中粘贴目标网页 URL
3. 点击 **「采集」** 按钮
4. 系统自动提取：
   - **标题**：网页 `<title>` + `og:title`
   - **描述**：`og:description` + meta description
   - **封面图**：`og:image`（自动下载并存储）
   - **正文**：Cheerio 提取主要文本内容
5. 提取完成 → 自动创建草稿 → 可编辑后发布

**URL 采集支持：**

- 普通网页（HTTP/HTTPS）
- 文章类网站（支持 og:meta 协议）
- 新闻门户（自动识别正文区）

### 6.3 草稿箱

> ![草稿箱](images/drafts.png)
> *图 22：草稿箱 — 所有草稿列表*

| 功能 | 说明 |
|------|------|
| **自动保存** | 编辑中的内容自动保存到草稿箱 |
| **草稿列表** | 按时间倒序排列，显示标题、摘要、保存时间 |
| **编辑** | 点击草稿进入编辑模式 |
| **删除** | 不需要的草稿可删除 |
| **一键发布** | 从草稿箱直接跳转到发布页 |

---

## 十三、内容创作

> 支持通过 PipelineBrowser 选择视频创作管线，详见用户手册 §9.

### 13.1 视频创作

> ![内容创作视图](images/create-content.png)
> *图 23：视频创作页面 — 文案生成与图片轮播*

**功能概述：**

内容创作模块支持快速生成视频内容，输入文案或上传图片素材即可自动渲染成视频。

**两种创作模式：**

| 模式 | 说明 | 适用场景 |
|:----:|------|---------|
| **📝 文案生成** | 输入场景文案，AI 自动配图并生成视频 | 教程、解说、资讯类内容 |
| **🖼️ 图片轮播** | 上传多张图片，自动生成轮播视频 | 产品展示、活动回顾 |

**操作步骤（文案生成）：**

1. 左侧导航 → **「🎬 视频创作」**
2. 选择「文案生成」模式
3. 输入视频文案（每行一个场景）
4. 选择输出平台（YouTube / 抖音 / B站等）
5. 选择视频主题风格
6. 点击 **「开始渲染」**
7. 实时查看渲染进度（进度条 + 百分比）
8. 渲染完成后预览并下载视频
9. 可一键跳转到发布页进行发布

### 13.1.1 图片轮播流水线（2026-08 更新）

#### 13.1.1.2 历史记录：状态、排序与详情

进入【视频创作 → 历史记录】时默认显示【全部】。页面提供【全部】【进行中】【已暂停】【执行失败】【已完成】【已取消】六个状态标签；标签只筛选列表，不会触发恢复。全部列表和每个状态列表都按任务有效更新时间倒序，最新发生变化的任务排在最前。

历史卡片在六个标签中使用同一结构，统一显示标题、流水线、状态、文案预览、首场景缩略图、更新时间/创建时间、任务耗时、视频时长、模式、任务标识和阶段摘要。标题为空时显示任务文案，再为空显示流水线名称或“未命名任务”；文案预览只取任务文案，超过 120 个 JavaScript 字符后追加 …，不使用图片/视频提示词代替。视频时长只表示成片时长，和流水线执行耗时分开显示。暂停任务还会显示暂停环节及暂停环境/检查点；失败任务显示失败环节和自然语言失败原因，文案会随界面语言显示中文或英文。

第一个场景的合法图片优先作为缩略图；没有合法图片时使用第一个合法视频的第 0 秒首帧。文件必须位于受控项目/媒体目录内，且不能是目录、符号链接、越界或超限文件。缩略图生成失败时保留空背景并显示“未生成”，不影响其他历史信息加载。

只要流水线已经启动且记录存在有效 `projectId`，已暂停、执行失败、已完成和已取消任务都可以通过卡片或【编辑】进入视频任务编辑页；进入不会自动继续、暂停、取消或删除。进行中任务仍回到流水线控制流程，暂停/后台运行/取消入口保持不变。没有 `projectId` 的纯流水线 run 只能查看状态或执行原有恢复/删除操作，不能伪造编辑页。已取消任务可以修改和保存已存在内容，但不能从断点继续。

视频任务编辑页为每个场景的图片、视频、图片/视频提示词、翻译、字幕和语音保留固定位置。素材尚未生成、生成失败、文件不存在或不可读时显示空的占位背景和“未生成”；用户仍可编辑其他字段、保存或重试，不会因为单个素材缺失而阻塞其他场景。

更新时间不只在完成时变化：保存文案、分段、素材、提示词、翻译、字幕或语音成功，以及暂停、从断点继续、取消、失败、完成状态写回成功后都会刷新。项目与流水线运行记录合并时，项目内容优先，运行记录补充状态/阶段/错误/运行耗时，更新时间取双方最新有效值。

【从断点继续】【继续生成】【打开结果】【删除】均为独立操作按钮，只有用户明确点击时才执行对应动作；点击操作按钮不会同时打开详情。任务轮询更新后列表会按相同规则重新排序。

> 当前【视频创作】以「流水线」形式运行，图片轮播（机器 ID `story2video-compose`）为全自动六阶段流水线：文案拆分 → 内容增强 → 画面提示词优化 → 图片/视频/旁白生成 → 合成轮播视频 → 发布（可选）。

**操作步骤（图片轮播）：**
1. 【视频创作】→ 选择「图片轮播」流水线。
2. 输入文案（最多 6000 字符），按需设置 图片风格/提示词风格/动效/字幕/分句/语音/音色/背景音乐/水印 等选项；选项修改后自动保存，下次进入自动恢复上次设置（顶部「恢复默认选项」可重置）。
3. 文件类选项附近会显示要求提示：图片（jpg/jpeg/png/webp，最大 10MB）、旁白音频（wav/m4a/mp3，最大 50MB）、背景音乐（wav/m4a/mp3，最大 15MB）、视频素材（mp4/mov/webm/mkv/avi，最大 512MB）。不符合要求时会弹出具体原因（格式/大小/不可读）。
4. 点击 **「启动流水线」** 全自动运行，无需人工干预；页面以「阶段进度清单」显示：文案拆分（拆分 N 个场景）、提示词优化（共 N 个场景，已完成 M 个）、图片/视频/旁白生成（开启 AI 视频时显示「图片 a/b · 视频 c/d · 旁白 e/f」，纯图片模式显示「图片 a/b · 旁白 c/d」）等；图片（非视频场景）与旁白无需等待 AI 视频完成即开始生成，三类工作并行推进，并显示各阶段耗时与整体进度/已用时。**「已用时」为流水线各步骤实际执行耗时总和**：暂停、检查点审阅以及失败后从断点恢复之间的空闲等待时间不计入；任务失败重试或断点续跑时，历史执行段会累计（2026-08-10 起）。
5. 运行中可返回流水线列表或切换到其他模块，流水线在后台继续；【历史记录】可查看运行中任务（点击回到流水线页查看实时进度）。
6. 完成后进入视频预览页：
   - **分段编辑**：可查看/编辑每段文案、提示词、图片，支持重试图片/视频、替换旁白、删除/移动分段、下载分段产物。
   - **下载视频**：点击后弹出系统保存对话框，选择保存位置即可保存成片（下载旁白/裁剪片段/分段图片/音频/视频同理）。
   - **导出 ZIP**：一键导出成片 + 旁白 + 全部分段产物。
7. 失败的任务会保留在【历史记录】中并显示状态「生成失败」；可从失败阶段「从断点继续」生成；遇到暂时的服务繁忙或网络波动时会自动等待后重试。

**常见提示说明：**
- 弹窗标题统一为「提示」；正文会给出具体原因与建议（如限流、额度不足、内容政策、模型未配置、文件不符合要求等）。
- 检测到模型服务响应异常（慢响应/超时）时，页面顶部会显示非阻塞提示，建议到【设置-模型设置】切换或检查对应服务商。

#### 13.1.1.1 水印选项（2026-08-14 更新）

> 2026-08-14 起，水印功能支持位置（含移动）、字号、透明度三个自定义选项，并修复了此前「填写了水印文字但成片看不到水印」的坐标问题。
> 2026-08-28 起，「移动」位置改为**成片级统一烧录**：多镜头成片跨镜头连续漂移、镜头切换不跳变，起点始终居中；单镜头行为不变。

**使用步骤：**
1. 在【视频创作】→「故事讲述」配置区，展开「视频增强」区域。
2. 勾选 **水印** 开关，在文字输入框填写水印文字（如品牌名、账号 ID）。
3. 按需选择：
   - **位置**：正中 / 左上 / 左下 / 右上 / 右下 / 移动（默认右下）。
   - **字号**：16 / 24 / 32 / 40 / 48（默认 24）。
   - **透明度**：10% ～ 100%，每档 10%（默认 60%）。
4. 点击 **「启动流水线」** 生成视频，成片画面中将显示水印；「移动」位置下水印从**画面中心附近**开始，在画面内平滑循环漂移（正弦轨迹），不会跑出画面。多镜头成片中，水印按**整片时间轴**连续漂移（成片级统一烧录），镜头切换处不跳变、不重置，漂移轨迹贯穿整片。

**说明与提示：**
- 水印文字与选项修改后自动保存，下次进入自动恢复上次设置；顶部「恢复默认选项」可将水印重置为默认值（右下 / 24 / 60%）。
- 四角位置（左上 / 左下 / 右上 / 右下）会自动预留画面边距（水平/底部 40px、顶部 60px），水印不会紧贴画面边缘；正中与移动不受边距影响。
- 「移动」位置为平滑的循环漂移（确定性轨迹）：从画面中心附近起步，沿正弦轨迹在画面内缓慢游走，每次生成轨迹一致，便于复现，不是随机跳动（2026-08-27 修正起点位置）。
- 旧版本保存的设置若包含已下线的位置值，会自动恢复到默认位置，不会导致选项空白。
- 水印仅在勾选开关且填写了文字时生效；不勾选或清空文字则成片无水印。

#### 13.1.2 场景上下文增强（自动中间层，2026-08-11）

> 技术细节见 `01-docs/PRD-STORY2VIDEO-SCENE-CONTEXT-2026-08-11.md` 与 `01-docs/ARCH-STORY2VIDEO-SCENE-CONTEXT-2026-08-11.md`。

【视频创作·图片轮播】流水线在「文案拆分」与「提示词优化」之间自动执行**场景上下文增强**（无需用户操作）：
- 系统自动阅读**完整文案**，理解故事背景（题材 / 时代朝代 / 文化地域 / 场景设定 / 角色 / 时代道具 / 视觉风格 / 叙事语气），并把背景锚点融合进**每一个场景**再交给提示词优化。
- 目的：避免背景漂移。例如全文讲中国唐代、某场景只写「一个老妇人在做饭」，系统会为该场景补充「唐代中国 · 长安民居厨房 · 土灶柴火」背景并排除现代电器（电烤箱/微波炉/西式厨房），生成更符合故事背景的图片/视频。
- 增强结果随流水线自动完成；无独立操作界面，可在【历史记录】与调试日志中查看分析结果（题材/时代/地域/锚点等）。
- 上下文增强失败时自动降级（按原文继续生成，不中断流水线）；仅在文案与场景都为空时提示「场景上下文增强需要非空文案与场景数组」。
- 规则（朝代/文化/题材/道具/负面锚点等）由**运营后台 → 场景上下文规则**页管理（查看/编辑/校验/保存/导出）；规则随桌面版本发布或放入本机用户配置目录覆盖加载。

### 13.2 AI 写稿

在文案输入区点击 **「AI 写稿」** 按钮，输入关键词或主题，系统自动生成完整的视频文案脚本。

### 13.3 输出配置

| 配置项 | 说明 |
|--------|------|
| **输出平台** | YouTube 横屏 (1920×1080)、抖音/TikTok (1080×1920)、B站 (1920×1080)、微信视频号 (1080×1920)、小红书 (1080×1440) |
| **视频主题** | 专业清晰、动感深色、简约清新、复古风格、科技感 |
| **分辨率** | 根据所选平台自动适配最佳分辨率 |
| **渲染引擎** | 基于 Remotion 的本地渲染引擎，支持离线渲染 |

---

## 十四、AI 辅助功能

### 14.1 AI 写作面板

> ![AI 辅助写作面板](images/ai-writer-panel.png)
> *图 24：AI 辅助写作面板 — 标题生成、内容润色、摘要生成*

**功能概述：**

AI 辅助写作面板提供三种智能写作模式，帮助创作者快速完成内容创作。

**前置条件：**

1. 点击左侧导航 → **「Provider 设置」**
2. 配置 LLM API Key（支持 OpenAI、Anthropic 等）
3. 配置完成后即可使用 AI 功能

### 14.2 标题生成

**操作步骤：**

1. 在发布页右侧打开 AI 写作面板
2. 选择 **「🎯 标题生成」** 模式
3. 输入文章主题或关键词
4. 点击 **「生成标题」**
5. 系统返回 3-5 个推荐标题
6. 点击某个标题自动填入发布内容

**生成示例：**

| 输入主题 | 推荐标题 |
|---------|---------|
| AI 工具推荐 | 2026 年不可错过的 10 款 AI 工具推荐 |
| AI 工具推荐 | 效率翻倍！2026 AI 工具实战测评 |
| AI 工具推荐 | 从入门到精通：AI 工具推荐指南 |

### 14.3 内容润色

**润色风格：**

| 风格 | 说明 | 适用平台 |
|:----:|------|---------|
| **专业严谨** | 用词精确，数据支撑 | 知乎、公众号 |
| **轻松活泼** | 口语化，加入 emoji | 小红书、微博 |
| **简洁精炼** | 短句为主，观点突出 | Twitter/X、微博 |
| **深度分析** | 结构化论证，案例丰富 | 知乎专栏、公众号 |

**操作步骤：**

1. 在 AI 写作面板中选择 **「✨ 内容润色」** 模式
2. 选择润色风格
3. 点击 **「润色正文」**
4. 系统返回润色后的内容
5. 点击 **「应用」** 替换原文

### 14.4 摘要生成

**操作步骤：**

1. 在 AI 写作面板中选择 **「📝 摘要生成」** 模式
2. 点击 **「生成摘要」**
3. 系统自动分析全文并提取关键信息
4. 生成的摘要可用于：
   - 发布时的文章摘要
   - 社交媒体分享描述
   - SEO 优化

---

## 十五、发布日历

### 15.1 功能概述

> ![发布日历视图](images/calendar-view.png)
> *图 25：发布日历 — 可视化内容排期*

发布日历提供可视化内容排期管理，帮助创作者规划和追踪各平台的发布计划。

**主要功能：**

- **月视图**：以月历形式展示每日发布任务
- **发布记录**：每格显示当日的发布记录数量
- **状态标识**：不同颜色圆点标识发布状态
- **日详情**：点击日期查看当日发布详情
- **快捷跳转**：点击「今天」快速回到当前日期

### 15.2 使用方式

1. 左侧导航 → **「📮 发布日历」**
2. 使用 ◀ ▶ 切换月份
3. 点击「今天」回到当前日期
4. 日历中带圆点的日期表示有发布记录
5. 点击任意日期 → 右侧面板显示该日发布详情

**状态标识：**

| 标识 | 含义 |
|:----:|------|
| 🟢 绿色圆点 | 发布成功 |
| 🔴 红色圆点 | 发布失败 |
| 🟣 紫色圆点 | 定时待发 |
| ⏰ 黄色背景 | 定时任务 |

### 15.3 排期管理

| 功能 | 说明 |
|------|------|
| **切换月份** | 支持查看过去和未来的发布记录 |
| **跳转今天** | 一键回到当前日期 |
| **查看详情** | 点击日期查看具体发布内容 |
| **快捷操作** | 从日历直接跳转到发布结果详情 |

---

## 十六、内容模板

> 功能自 v2.0.1 起可用
> ![内容模板](images/template-picker.png)
> *图 23：模板选择面板 — 按分类分组展示*

### 16.1 使用模板

1. 在发布页面点击 **「📝 模板」** 按钮
2. 模板面板打开，按分类分组展示：
   - **通用模板**：新闻稿、产品介绍、活动通知等
   - **各平台模板**：微信公众号、知乎、小红书等特定格式
   - **我的模板**：用户自定义模板
3. 点击模板名称 → 自动填充标题和内容
4. 填充后可根据需要修改再发布

### 16.2 管理模板

| 操作 | 方法 |
|------|------|
| **创建模板** | 模板管理页面 → 输入名称、选择分类、编辑内容 → 保存 |
| **编辑模板** | 点击模板 → 编辑内容 → 保存 |
| **删除模板** | 点击模板旁的删除按钮 |
| **内置模板** | 系统提供常用预设（不可删除） |

---

## 十七、发布任务管理

### 17.1 任务队列

> ![任务队列](images/task-queue.png)
> *图 24：任务队列 — 实时显示发布任务状态*

所有发布请求进入统一的任务队列管理：

| 特性 | 说明 |
|------|------|
| **并发数** | 最多 3 个任务同时执行（可配置） |
| **FIFO 队列** | 先进先出，等待中的任务按序执行 |
| **自动重试** | 失败任务自动重试（默认 3 次，可配置） |
| **任务取消** | 等待中或执行中的任务可随时取消 |
| **崩溃恢复** | 进程崩溃后重启自动恢复未完成队列 |

### 17.2 实时进度

> ![发布进度](images/publish-progress.png)
> *图 25：实时发布进度 — 显示当前阶段和状态*

发布进度通过 IPC 实时推送到前端，显示以下信息：

| 信息 | 说明 |
|:----:|------|
| **当前阶段** | 如「正在登录 xx 平台」「正在填写标题」「正在上传图片」「正在发布」 |
| **进度条** | 直观显示整体完成度 |
| **状态标签** | 🟡 进行中 / ✅ 成功 / ❌ 失败 / ⏳ 等待中 |
| **详情** | 点击查看详细日志 |

### 17.3 发布频率控制

> 功能自 v2.1.0 起可用

为防止平台限流封号，系统内置 **PublishIntervalGuard**：

| 规则 | 说明 |
|------|------|
| **间隔限制** | 同一账号两次发布至少间隔 **5 分钟** |
| **自动等待** | 被拦截的任务自动延迟，等待间隔过后重试 |
| **跨账号隔离** | 不同账号同一平台不受影响 |
| **跨平台隔离** | 同一账号不同平台不受影响 |
| **持久化** | 发布时间记录写入 SQLite，重启不丢失 |

### 17.4 发布计划

> 功能自 v1.6.1 起可用

> ![发布计划](images/publish-plan.png)
> *图 26：发布计划 — 内容日历视图*

**内容日历视图：**

- 以日历形式展示发布计划
- 每个日期显示已安排的任务数
- 拖拽调整发布时间
- 支持一键排期（设置 → 自动分配到最佳时间）

### 17.5 结果通知

| 通知类型 | 表现 |
|---------|------|
| **发布成功** | 系统通知弹出 + 状态更新 |
| **发布失败** | 系统通知弹出 + 托盘图标闪烁 + 失败原因展示 |
| **重试通知** | 通知正在重试及重试次数 |

---

## 十八、分屏监控

### 18.1 功能概述

> ![分屏监控](images/monitor-split.png)
> *图 27：4 分屏监控 — 同时查看多个平台*

分屏监控功能让你在一个界面同时查看多个平台的内容动态，适合运营人员实时追踪各平台数据。

### 18.2 布局选择

| 布局 | 分屏数 | 适用场景 |
|:----:|:------:|---------|
| 2 分屏 | 2 | 对比两个平台 |
| 3 分屏 | 3 | 监控主要平台 |
| 4 分屏 | 4 | 常用监控布局 |
| 6 分屏 | 6 | 大量平台同时监控 |

### 18.3 使用步骤

1. 左侧导航 → **「📡 分屏监控」**
2. 选择布局（2/3/4/6）
3. 每个分屏点击「添加」→ 选择平台
4. 对于未登录的平台，使用内嵌浏览器登录
5. 每个分屏拥有**独立的 Cookie/Session 隔离**，互不干扰

### 18.4 实时回调

| 特性 | 说明 |
|------|------|
| **回调端口** | 本地 HTTP 服务器 `:16521` |
| **心跳** | 每 59 秒心跳检测 |
| **数据记录** | 评论/互动数据自动写入 SQLite `callback_logs` 表 |
| **前端展示** | 实时展示回调数据更新 |

---

## 十九、评论管理

> 功能自 v1.1.0（融媒宝 F4）起可用

### 19.1 功能概述

> ![评论管理](images/comments-page.png)
> *图 28：评论管理 — 统一管理各平台评论*

评论管理（Comments.vue）采用 WebContentsView 内嵌各平台评论页面，在一个界面统一查看和管理多个平台的用户评论。

### 19.2 支持平台

目前支持评论管理的平台：

- 微信公众号
- 知乎
- 微博
- 抖音
- 小红书

### 19.3 使用方式

1. 左侧导航 → **评论管理**（Provider 菜单下）
2. 顶部 Tab 切换不同平台
3. 每个 Tab 内嵌该平台的评论管理页面
4. 可直接在应用内回复评论
5. 评论数据自动同步到本地 SQLite

---

## 二十、发布历史与统计

### 20.1 发布历史

> ![发布历史](images/publish-history.png)
> *图 29：发布历史 — 按时间倒序排列*

| 功能 | 说明 |
|------|------|
| **列表展示** | 所有发布记录按时间倒序排列 |
| **筛选** | 按平台、时间范围、发布状态筛选 |
| **详情** | 点击查看每条发布的详细信息（内容、平台、耗时、错误信息等） |
| **重新发布** | 失败的发布可一键重新发布 |

### 20.2 统计看板

> ![统计看板](images/dashboard.png)
> *图 30：统计看板 — 发布数据可视化*

| 模块 | 展示内容 |
|:----:|---------|
| **总览卡片** | 总发布数、今日发布数、成功率、7 日趋势 |
| **平台分布** | 各平台发布数量柱状图/饼图 |
| **趋势图** | 发布量随时间变化的折线图（7 天 / 30 天 / 自定义） |
| **成功率** | 各平台发布成功率对比 |
| **时段分析** | 一天中各时段的发布量分布 |

### 20.3 发布后监控

> 功能自 v1.0.13 起可用

发布完成后，系统自动轮询平台审核状态：

| 阶段 | 状态 |
|:----:|------|
| ⏳ 等待审核 | 发布成功，等待平台审核 |
| ✅ 审核通过 | 内容已成功发布 |
| ❌ 审核拒绝 | 内容未通过审核（显示原因） |
| 🔄 持续监控 | T+1min / 1h / 24h 持续检查 |

---

## 二十一、内容情报引擎

> 功能自 v1.4.0 起可用
> 入口：左侧导航栏 **「🧠 情报中心」**

> ![情报中心首页](images/intelligence-home.png)
> *图 41：情报中心 — 6 个 Tab 覆盖创作全流程*

### 21.1 策题灵感（Trending）

> 🔥 **热门趋势面板** — 跨平台热点内容聚合（Reddit / HN / GitHub）

> ![热门趋势面板](images/trending-panel.png)
> *图 43：热门趋势面板 — 跨平台热点发现*

**功能概述：**

热门趋势面板（TrendingPanel）汇聚 Reddit、Hacker News、GitHub 等平台的热门内容。

**数据来源：**

| 来源 | 图标 | 内容类型 | 排序算法 |
|:----:|:----:|---------|---------|
| **Reddit** | 🔴 | 各子版块热门讨论 | upvotes + 评论数 |
| **Hacker News** | 🟠 | 技术热门文章 | points 评分 |
| **GitHub** | ⚫ | Trending 仓库 | stars 增长速率 |

**使用方式：**

1. 情报中心 → **「策题灵感」** Tab
2. 顶部 Tab 切换来源（全部 / Reddit / HN / GitHub）
3. 浏览聚合的热门话题列表
4. 点击感兴趣的话题 → 查看详情
5. 一键创建为草稿 → 进入编辑发布

### 21.2 标题优化

> ![标题优化](images/intelligence-title-optimizer.png)
> *图 33：标题优化 — 搜索同类标题互动数据*

详见 [5.5 标题优化助手](#55-标题优化助手)。

### 21.3 标签建议（Tag Suggester）

> ![标签建议](images/intelligence-tags.png)
> *图 34：标签建议 — 自动生成各平台推荐标签*

**功能：**

1. 输入正文关键词
2. 系统分析内容主题
3. 结合各平台标签体系，自动生成推荐标签
4. 每个平台独立生成，适配平台标签规则
5. 点击标签一键添加到发布内容

### 21.4 引用推荐（Reference Finder）

> ![引用推荐](images/intelligence-references.png)
> *图 35：引用推荐 — 自动搜索权威来源*

**操作步骤：**

1. 在正文中选中关键词或句子
2. 点击 **「🔍 搜索引用」** 按钮
3. 系统自动搜索权威来源：
   - 学术论文（CrossRef）
   - 新闻报道
   - 行业数据
   - 维基百科
4. 选择引用源 → 一键插入正文

### 21.5 最佳发布时间

> ![最佳发布时间分析](images/optimal-time.png)
> *图 47：最佳发布时间分析 — 基于 Reddit/HN/GitHub 数据的推荐*

**功能概述：**

最佳发布时间提示（OptimalTimeTip）通过分析 Reddit、HN、GitHub 等平台的同类内容互动数据，智能推荐最佳发布时段。

**数据驱动分析：**

| 数据源 | 分析指标 | 数据量 |
|:------:|---------|:------:|
| **Reddit** | 帖子 upvotes + 评论数 | 452 条 |
| **Hacker News** | HN points | 386 条 |
| **GitHub** | Stars + 活跃度 | 446 条 |
| **综合** | 跨平台加权评分 | 1,284 条 |

**推荐时段示例：**

| 时段 | 评分 | 推荐级别 | 说明 |
|:----:|:----:|:--------:|------|
| **20:00** | **10.0** | 🟢 最佳 | 同类互动率最高时段 |
| 08:00 | 8.2 | 🟢 良好 | 早高峰阅读时段 |
| 12:00 | 7.5 | 🟡 中等 | 午间休息时段 |
| 18:00 | 6.8 | ⚪ 一般 | 通勤时段 |

**操作步骤：**

1. 在发布页面的定时发布设置旁，输入目标关键词
2. 系统自动分析并展示推荐发布时段
3. 点击推荐时间 → 自动填入定时发布设置
4. 也可在情报中心 **「发布时机」** Tab 使用

### 21.6 影响力追踪

> ![影响力追踪](images/impact-tracker.png)
> *图 37：发布后影响力追踪 — 3 天持续监控*

发布完成后自动追踪内容在各平台的影响力：

| 时间节点 | 追踪内容 |
|---------|---------|
| T+1 分钟 | 确认发布状态 |
| T+1 小时 | 早期互动数据（阅读量、点赞数） |
| T+24 小时 | 热度评估（转发、评论、收藏） |
| T+72 小时 | 完整影响力报告 |

**Dashboard 展示：**

- 单条内容的影响力趋势折线图
- 跨平台对比表现
- 与同类内容的基准对比

### 21.7 关键词监测

> ![关键词监测](images/keyword-monitor.png)
> *图 38：关键词监测 — 实时追踪热词*

**功能：**

1. 在情报中心 **「关键词监测」** Tab 添加要追踪的关键词
2. 系统持续监测 Reddit/HN/GitHub 等来源的讨论热度
3. 热度异常飙升时触发桌面通知
4. 可配置监测频率和通知阈值

**关键词监测列表：**

| 关键词 | 当前热度 | 7 日趋势 | 操作 |
|--------|:--------:|:--------:|:----:|
| AI 写作 | ██████░ | 📈 上升 | 查看详情 |
| 自媒体 | ████░░░ | 📊 平稳 | 查看详情 |
| 小红书运营 | ███████ | 📈 飙升 🔥 | 查看详情 |

### 21.8 基准比较

> ![基准比较](images/benchmark-chart.png)
> *图 39：内容表现基准比较 — 与同类内容对比*

将自己的内容表现与同类内容的基准数据进行对比：

| 指标 | 你的内容 | 同类基准 | 差距 |
|:----:|:--------:|:--------:|:----:|
| 平均阅读量 | 5,230 | 3,840 | +36% ✅ |
| 点赞率 | 4.2% | 3.1% | +35% ✅ |
| 评论数 | 28 | 15 | +87% ✅ |
| 分享率 | 1.8% | 2.2% | -18% ⚠️ |

---

## 二十二、爆款分析

> 功能自 v1.4.0 起可用
> 入口：情报中心 → **「爆款分析」** Tab

### 22.1 功能概述

> ![爆款分析](images/viral-analysis.png)
> *图 40：爆款分析 — 识别高互动内容模式*

ViralEngine（viral-engine.js）分析各平台高互动内容，识别爆款模式：

| 分析维度 | 说明 |
|---------|------|
| **标题模式** | 爆款内容的标题关键词和句式分析 |
| **内容长度** | 最佳字数范围统计 |
| **发布时间** | 爆款内容的发布时间规律 |
| **标签分析** | 高频标签和标签组合 |
| **情感倾向** | 内容的正面/负面/中性情绪分布 |

### 22.2 使用方式

1. 情报中心 → **「爆款分析」** Tab
2. 选择分析平台和目标领域
3. 系统展示分析结果
4. 根据分析建议调整你的内容策略

### 22.3 内容表现基准比较

在 Dashboard 中可以查看你的内容与同类爆款内容的基准对比，提供差距分析和优化建议。

---

## 二十三、云端发布

> 功能自 v1.5.0 起可用
> 入口：左侧导航 → **「☁️ 云端发布」**

### 23.1 功能概述

> ![云端发布页面](images/cloud-publish.png)
> *图 41：云端发布 — 将发布任务提交到 ECS 云服务器执行*

云端发布允许将发布任务提交到 ECS 云服务器执行，无需本地持续运行：

| 特性 | 说明 |
|------|------|
| **提交发布** | 本地编辑内容 → 提交到云端 → 云服务器执行 RPA |
| **任务列表** | 查看所有已提交的云端任务 |
| **进度轮询** | 前端定时轮询任务状态 |
| **即关即走** | 提交后关闭 App 不影响任务执行 |

### 23.2 支持的平台

目前支持云端发布的平台：

- **B站**（bilibili_publisher.py — Cookie-based 直连发布）
- **抖音**（douyin_publisher.py — Cookie-based 直连发布）

### 23.3 使用方式

1. 左侧导航 → **「☁️ 云端发布」**
2. 选择发布模式：
   - **`mode: "rpa"`**：本地 RPA 执行（默认）
   - **`mode: "cloud"`**：提交到云端执行
3. 编辑内容 → 选择平台 → 点击发布
4. 任务提交到云端 → 可在任务列表查看进度
5. 关闭 App 后重新打开 → 任务继续执行不受影响

---

## 二十四、内容质量检测

### 24.1 敏感词预检

> ![敏感词检测](images/sensitive-filter.png)
> *图 42：敏感词检测 — 发布前自动扫描*

**功能：**

- 基于 **DFA 算法**（确定性有限自动机）高效敏感词匹配
- 内置开源敏感词库
- 支持自定义添加敏感词

**触发方式：**

- **自动触发**：点击发布按钮前自动扫描内容
- **手动触发**：在编辑器中点击「🔍 检测敏感词」

**检测结果：**

| 级别 | 颜色 | 处理方式 |
|:----:|:----:|---------|
| 🚫 违规 | 红色高亮 | 弹窗警告，修改后才能发布 |
| ⚠️ 建议 | 黄色高亮 | 弹窗提示，可忽略继续发布 |
| ✅ 通过 | 绿色 | 无提示，正常发布 |

### 24.2 内容质量门禁

> 功能自 v1.1.0 起可用

ContentQualityGate（content-quality-gate.js）在发布前自动检查内容质量：

| 检查项 | 标准 |
|--------|------|
| **标题长度** | 各平台要求不同，自动适配 |
| **正文长度** | 最少 50 字，建议 300+ 字 |
| **图片数量** | 至少 1 张封面图 |
| **格式检查** | HTML 标签闭合、编码正确性 |
| **重复内容** | 检测与历史发布内容的相似度 |

---

## 二十五、数据同步

> 功能自 v1.1.0（融媒宝 F3）起可用

### 25.1 功能概述

> ![数据同步](images/data-sync.png)
> *图 43：数据同步 Dashboard — 多平台数据聚合*

DataSync 系统（data-sync.js）框架自动同步各平台的数据到本地 SQLite：

| 平台 | 同步数据 |
|------|---------|
| 微信公众号 | 粉丝数、文章阅读量 |
| 知乎 | 赞同数、关注者 |
| 微博 | 粉丝数、阅读量 |
| 抖音 | 粉丝数、点赞数 |
| 小红书 | 粉丝数、笔记数据 |

### 25.2 Dashboard 数据卡片

在 Dashboard 页面，每个平台以数据卡片形式展示：

```
┌──────────────────────┐
│  📱 微信公众号          │
│  👥 粉丝：12,350       │
│  📈 +230 (本周)        │
│  上次同步：5 分钟前      │
└──────────────────────┘
```

### 25.3 Provider 管理

> ![Provider 管理](images/providers-page.png)
> *图 44：Provider 管理 — 配置各平台数据源*

在 Providers.vue 页面配置各平台的数据提供者：

- 每个平台注册独立的数据获取函数（Provider）
- 单平台数据获取 + 多平台并行概览
- 指标归一化处理
- 错误隔离（一个平台失败不影响其他平台）

---

## 二十六、插件系统

> 功能自 v1.8.0 起可用
> 入口：左侧导航 → **「🔌 插件管理」**

### 26.1 概述

> ![插件管理页面](images/plugins-page.png)
> *图 45：插件管理 — 已安装插件列表*

插件系统允许开发者通过编写 JavaScript 文件来扩展 Multi-Publish 的支持平台，无需修改主程序源码。

### 26.2 安装插件

| 步骤 | 操作 |
|:----:|------|
| ① | 在应用数据目录下创建 `plugins/` 文件夹 |
| ② | 将插件 `.js` 文件（或包含 `index.js` 的目录）放入 |
| ③ | 重启 App 或点击「重新加载插件」 |
| ④ | 在「发布」页面即可看到新增的平台选项 |

### 26.3 插件开发规范

```javascript
// 完整的插件示例
module.exports = {
  // 必填：平台唯一标识
  platform: 'my_platform',

  // 必填：界面显示名称
  displayName: '我的平台',

  // RPA 发布模式（选填，至少实现一种发布方式）
  async publish(page, article) {
    // page: RpaViewManager 的 BrowserWindow 实例
    // article: { title, content, images[], coverUrl }
    // 编写 DOM 操作逻辑...
    return { success: true, url: 'https://...' };
  },

  // API 发布模式（选填）
  async publishViaApi(article) {
    // 通过 HTTP API 发布
    const response = await fetch('https://api.example.com/publish', {
      method: 'POST',
      body: JSON.stringify(article)
    });
    return response.json();
  },

  // 登录验证（选填）
  async validate(cookies) {
    // 返回 true/false 表示 Cookie 是否有效
    return true;
  }
};
```

> 详细开发指南见 `docs/plugin-development-guide.md`。

### 26.4 插件安全

| 安全机制 | 说明 |
|---------|------|
| **错误隔离** | 单个插件崩溃不影响主程序 |
| **重复检测** | 同名平台自动检测，内置适配器优先级高于插件 |
| **元数据查询** | 可查询插件的名称、版本、作者等信息 |

---

## 二十七、API 开放平台

> 功能自 v1.7.0 起可用

### 27.1 概述

> ![API 管理页面](images/api-management.png)
> *图 46：API 管理 — 创建和管理 API Key*

API 开放平台允许开发者通过 REST API 调用 Multi-Publish 的发布能力，适用于 CI/CD 集成、自动化工作流等场景。

### 27.2 API Key 管理

**创建 Key：**

1. 设置 → **「API 管理」**
2. 点击 **「创建 API Key」**
3. 设置权限范围（scope）：
   - `*` 通配符：全部权限
   - `publish:read`：只读发布任务
   - `publish:write`：提交发布任务
   - `admin`：管理权限（创建/吊销 Key）
4. 生成的 Key 格式：`mp_` + 48 位十六进制字符
5. **复制并保存 Key**（关闭后不再显示原文）

**管理 Key：**

| 操作 | 端点 | 说明 |
|------|------|------|
| 创建 | `POST /api/v1/keys` | 新建 Key（需 admin scope） |
| 列表 | `GET /api/v1/keys` | 查看所有 Key（不暴露原文） |
| 吊销 | `DELETE /api/v1/keys/:key` | 吊销指定 Key |

### 27.3 API 端点

| 方法 | 端点 | 说明 |
|:----:|------|------|
| GET | `/api/v1/platforms` | 获取支持平台列表 |
| POST | `/api/v1/publish` | 提交发布任务 |
| GET | `/api/v1/jobs/:id` | 查询任务状态 |
| POST | `/api/v1/keys` | 创建 API Key（admin） |
| GET | `/api/v1/keys` | 列出 API Key（admin） |
| DELETE | `/api/v1/keys/:key` | 吊销 API Key（admin） |

### 27.4 SDK 使用

```bash
npm install @multi-publish/api-publish-engine
```

```javascript
const { PublishApiClient } = require('@multi-publish/api-publish-engine');

const client = new PublishApiClient({
  apiKey: 'mp_abc123...',
  baseUrl: 'http://localhost:16521' // 默认
});

// 获取平台列表
const platforms = await client.getPlatforms();

// 提交发布任务
const job = await client.publish({
  platform: 'weixin',        // 平台标识
  title: '文章标题',
  content: '# Markdown 正文...',
  coverUrl: 'https://...',   // 封面图 URL（可选）
  scheduledAt: '2026-07-10T10:00:00Z' // 定时发布（可选）
});

// 查询任务状态
const status = await client.getJobStatus(job.id);
console.log(status); // { id, status: 'running', progress: 60 }
```

### 27.5 Webhook

> 功能自 v1.6.1 起可用

配置 Webhook 接收发布状态变更通知：

| 设置项 | 说明 |
|--------|------|
| **Webhook URL** | 接收通知的 HTTP 端点 |
| **触发事件** | `publish.success` / `publish.failed` / `publish.started` |
| **签名验证** | 请求带 `X-Signature` 头部验证来源 |

**Webhook Payload 格式：**

```json
{
  "event": "publish.success",
  "timestamp": "2026-07-08T10:00:00Z",
  "data": {
    "jobId": "job_xxx",
    "platform": "weixin",
    "title": "文章标题",
    "url": "https://mp.weixin.qq.com/s/..."
  }
}
```

### 27.6 审计日志

> 功能自 v1.6.1 起可用

所有 API 调用自动记录审计日志：

| 记录项 | 说明 |
|--------|------|
| **时间戳** | 请求时间 |
| **API Key** | 使用的 Key 标识 |
| **端点** | 请求路径 |
| **IP 地址** | 请求来源 |
| **操作结果** | 成功/失败 + 状态码 |

审计日志存储在 `config/audit.log` 文件中。

---

## 二十八、许可证与定价

### 28.1 版本对比

| 特性 | 免费版 | Pro 版 |
|------|:------:|:------:|
| 基础发布功能 | ✅ | ✅ |
| 支持平台数 | 15 个 | 15 个 |
| 使用设备数 | 1 台 | 3 台 |
| 内容模板 | 基础模板 | 全部模板 |
| 标题优化助手 | — | ✅ |
| 内容情报引擎 | — | ✅ |
| 爆款分析 | — | ✅ |
| 插件系统 | — | ✅ |
| API 开放平台 | — | ✅ |
| Webhook | — | ✅ |
| 优先级支持 | — | ✅ |

### 28.2 激活方式

> ![升级 Pro 弹窗](images/trial-upgrade.png)
> *图 59：升级 Pro 弹窗 — 方案对比 + 支付方式选择*

**试用激活：**

1. 新用户自动获得 **7 天 Pro 试用**
2. 试用期间可体验全部 Pro 功能（包括 AI 写作、数据分析、API 等）
3. 试用到期后自动降级为免费版
4. 降级后已保存的数据不会丢失

> ![试用+升级界面](images/trial-upgrade.png)
> *图 60：试用横幅与升级弹窗 — 方案对比 + 支付方式选择*

**正式购买激活：**

1. 点击界面右上角 **「升级 Pro」** 按钮（¥99 永久授权）
2. 在弹窗中查看免费版 vs Pro 版功能对比
3. 点击 **「立即升级」**
4. 选择支付方式（支付宝 / 微信支付）
5. 扫码完成支付后自动激活
6. 激活成功后立即生效，无需重启

**开发模式（模拟支付）：**

| 步骤 | 操作 |
|:----:|------|
| ① | 在升级弹窗中点击「立即升级」 |
| ② | 选择支付方式（支付宝/微信）|
| ③ | 点击「确认支付 ¥99」|
| ④ | 在弹出的二维码页面点击「模拟支付成功」|
| ⑤ | 系统自动激活 Pro 权限 |

**激活码激活（企业用户）：**

1. 在升级弹窗中切换到 **「激活码」** Tab
2. 输入企业发放的激活码
3. 点击「激活」→ 激活成功后立即生效

**试用横幅：**

| 显示状态 | 触发条件 | 操作按钮 |
|---------|---------|---------|
| 🟡 试用中 | 试用期还剩 1-6 天 | 「立即购买」 |
| 🔴 即将到期 | 试用期最后 1 天 | 「立即购买」（强调显示） |
| ⚪ 免费版 | 试用已过期或未试用 | 「升级 Pro」

> ![试用横幅](images/trial-upgrade.png)
> *图 60：试用横幅 — 显示剩余试用天数与升级入口*

**正式激活：**

1. 点击界面右上角 **「升级 Pro」** 按钮
2. 在弹窗中输入激活码
3. 点击「激活」→ 激活成功后立即生效

### 28.3 许可证管理

| 操作 | 说明 |
|------|------|
| **查看状态** | 设置页显示当前许可证类型和有效期 |
| **跨设备激活** | Pro 最多 3 台设备，在设置页管理已激活设备 |
| **许可证存储** | 加密存储为 `license.json`（XOR base64 防篡改） |
| **降级机制** | 过期自动降级，功能受限但不丢失数据 |

### 28.4 使用统计

> 功能自 v2.0.1 起可用

UsageTracker 记录应用使用数据：

| 统计项 | 说明 |
|--------|------|
| **发布次数** | 总发布次数（免费版限制每日次数） |
| **各平台使用量** | 统计各平台的使用频率 |
| **功能使用率** | 各功能模块的使用情况 |
| **存储管理** | 本地存储空间使用情况 |

---

## 二十九、系统功能

### 29.1 系统托盘

> ![系统托盘](images/system-tray.png)
> *图 63：系统托盘菜单*

| 操作 | 说明 |
|------|------|
| **最小化到托盘** | 关闭窗口时自动收起到系统托盘 |
| **后台运行** | 后台继续执行发布任务 |
| **托盘菜单** | 右键点击托盘图标 → 显示/隐藏、查看进度、退出 |
| **闪烁告警** | 发布失败时托盘图标闪烁提醒 |

### 29.2 全局快捷键

| 快捷键 | 功能 |
|:------:|------|
| `Ctrl + Alt + P` | 打开发布页面 |
| `Ctrl + Alt + M` | 打开分屏监控 |
| `Ctrl + Alt + D` | 打开统计看板 |
| `Ctrl + Alt + C` | 打开内容采集 |
| `Ctrl + Alt + H` | 回到首页 |
| `Ctrl + Alt + Q` | 退出应用 |
| `Ctrl + K` / `⌘K` | 打开命令面板 |

### 29.3 命令面板

> ![命令面板](images/command-palette.png)
> *图 62：命令面板 — 快速搜索和执行命令*

**功能概述：**

命令面板提供类似 VS Code 的快速命令搜索与执行体验。

**操作方式：**

| 操作 | 说明 |
|:----:|------|
| **打开** | `Ctrl + K` / `⌘K` |
| **搜索命令** | 输入关键词模糊搜索 |
| **执行命令** | 点击命令项或按 `Enter` |
| **关闭** | `Esc` 或点击面板外区域 |

**可用命令列表：**

| 命令 | 快捷键 | 功能说明 |
|:----:|:------:|---------|
| 打开发布页 | ⌘P | 跳转到一键发布页面 |
| 打开监控 | ⌘M | 打开分屏监控视图 |
| 打开数据看板 | ⌘D | 查看发布统计和趋势 |
| 打开内容采集 | ⌘C | 进入内容采集页面 |
| 打开发布日历 | ⌘K | 查看内容排期 |
| 新建内容 | ⌘N | 创建新的发布内容 |
| 回到首页 | ⌘H | 回到应用首页 |

### 29.4 自动更新

| 阶段 | 说明 |
|:----:|------|
| **启动检测** | App 启动 3 秒后自动检测 GitHub Release |
| **发现新版本** | 弹出更新对话框，显示版本号和更新内容 |
| **下载更新** | 点击「下载更新」→ 后台下载（支持断点续传） |
| **安装更新** | 下载完成 → 「立即重启安装」 |
| **GFW 静默** | 国内网络连接 GitHub 失败时静默处理，不弹错误提示 |

### 29.5 离线模式

> 功能自 v2.0.0 起可用

| 场景 | 行为 |
|:----:|------|
| **断网检测** | 系统自动检测网络连接状态 |
| **自动切换** | 断网时自动切换离线模式 |
| **操作缓存** | 离线期间的发布操作缓存到本地 |
| **自动同步** | 网络恢复后自动执行缓存的发布任务 |

**离线状态标识：**

- 侧栏底部显示离线状态图标 🌐
- 离线时发布操作不受影响，任务排队等待网络恢复

### 29.6 国际化

> 功能自 v2.0.0 起可用

| 语言 | 支持状态 |
|:----:|:--------:|
| 🇨🇳 简体中文 | ✅ 完整支持 |
| 🇺🇸 English | ✅ 完整支持 |

**切换方式：**

1. 设置 → **「语言」**
2. 选择 `中文` 或 `English`
3. 切换后即时生效，无需重启

### 29.7 反检测技术

> ![反检测设置](images/stealth-settings.png)
> *图 64：反检测设置 — 浏览器指纹保护*

为降低 RPA 操作被平台识别的风险，系统内置反检测机制：

| 技术 | 说明 |
|------|------|
| **浏览器指纹混淆** | stealth-helper.js 修改 WebDriver 属性、navigator 特征 |
| **人类行为模拟** | 随机延迟、鼠标轨迹模拟、输入间隔随机化 |
| **随机 User-Agent** | 每次 Session 使用不同的 User-Agent |
| **代理支持** | 支持 HTTP/HTTPS/SOCKS 代理轮换 |

### 29.8 代理配置

> 功能自 v2.1.0 起可用

ProxyPool（proxy-pool.js）提供代理池管理：

| 功能 | 说明 |
|------|------|
| **手动配置** | 在设置页配置单个 HTTP/HTTPS/SOCKS 代理 |
| **代理轮换** | 配置多个代理自动轮换 |
| **健康检测** | 自动检测代理可用性 |
| **失败切换** | 代理不可用时自动切换到下一个 |

### 29.9 日志系统

| 日志类型 | 位置 | 用途 |
|---------|------|------|
| **应用日志** | `logs/app.log` | 应用运行日志 |
| **RPA 日志** | `logs/rpa.log` | RPA 引擎操作日志 |
| **API 审计日志** | `config/audit.log` | API 调用审计记录 |
| **发布日志** | SQLite `publish_history` | 发布历史记录 |

---

## 三十、常见问题

### 21.1 安装与启动

**Q: 安装包下载很慢怎么办？**

A: 推荐以下方式：
- 使用 **Gitee 国内镜像**：https://gitee.com/ColinChiu007/Multi-Publish/releases（5MB/s）
- 使用 `gh-proxy.com` 加速：`https://gh-proxy.com/https://github.com/Colinchiu007/Multi-Publish/releases/download/v1.1.0/Multi-Publish.Setup.1.1.0.exe`

**Q: 首次启动很慢，卡在"检测依赖"？**

A: 首次启动需要下载内置浏览器组件（~170MB），请耐心等待 1-2 分钟。如果超过 5 分钟无响应：
1. 检查网络连接
2. 检查是否被防火墙拦截
3. 重启 App 重试
4. 首次运行后，后续启动秒开

**Q: macOS 提示"无法验证开发者"？**

A: 由于未进行 Apple 开发者签名，首次打开需要：
1. 右键点击 App → 选择「打开」
2. 在弹出的对话框中选择「打开」
3. 之后即可正常打开

**Q: Linux 下 AppImage 无法运行？**

A: 执行 `chmod +x Multi-Publish-*.AppImage` 赋予执行权限后运行。如果提示 FUSE 错误，添加 `--no-sandbox` 参数。

### 21.2 账号相关

**Q: 添加账号时浏览器窗口不弹出？**

A: 检查：
1. 是否被防火墙拦截
2. 操作系统弹窗权限是否开启
3. 是否有其他 Electron 应用占用端口

**Q: 登录状态经常过期怎么办？**

A: 各平台登录有效期不同（微信扫码通常 7-30 天，账号密码可能 1-3 天）。建议：
- 定期检查账号状态（状态指示灯 🟢=正常 🟡=即将过期 🔴=已过期）
- 收到重新登录提示时及时处理
- 微信公众号使用扫码登录有效期更长

**Q: Cookie 安全吗？会被上传到服务器吗？**

A: 绝对安全：
- 所有 Cookie 使用 **AES-256-GCM** 加密后存储在本地文件
- **不会上传到任何远程服务器**
- 每个账号使用 `session.fromPartition()` 独立 Cookie 分区
- 加密密钥从本地机器特征派生

**Q: 最多能添加多少个账号？**

A: 没有数量限制。但建议单平台不超过 10 个账号，过多的账号同时发布可能被平台识别。

### 21.3 发布相关

**Q: 发布失败怎么办？**

A: 按以下步骤排查：

| 步骤 | 检查项 | 解决方法 |
|:----:|--------|---------|
| ① | 账号登录状态 | 重新登录过期账号 |
| ② | 查看失败原因 | 任务详情中显示具体错误信息 |
| ③ | 内容格式 | 检查是否有不兼容的格式 |
| ④ | 敏感词 | 运行敏感词检测 |
| ⑤ | 重试 | 修复后点击「重新发布」 |

**常见失败原因：**

| 原因 | 处理方式 |
|------|---------|
| Cookie 过期 | 重新登录 |
| 内容违规（敏感词） | 修改内容后重试 |
| 平台限流 | 等待 5 分钟后重试 |
| 网络异常 | 检查网络连接 |
| 平台 UI 变更 | 更新到最新版本 |

**Q: 视频上传支持哪些格式和大小？**

A: 支持 `mp4`、`mov`、`avi` 格式，建议不超过 500MB。各平台对视频时长和分辨率有不同限制，发布前请参考平台官方要求。

**Q: 定时任务重启后还在吗？**

A: 在。定时任务持久化到 SQLite 数据库，App 重启后自动恢复。

**Q: 为什么提示"发布间隔太短"？**

A: 同一账号两次发布之间至少需要间隔 **5 分钟**，这是为了防止被平台限流封号。等待 5 分钟后自动重试。

**Q: 批量发布时，一个平台失败会影响其他平台吗？**

A: **不会**。每个平台的发布任务互相独立，一个平台失败不影响其他平台的发布。

### 21.4 数据与存储

**Q: 数据存储在哪里？**

A: 所有数据存储在本地应用数据目录：

| 系统 | 默认路径 |
|:----:|---------|
| Windows | `%APPDATA%/Multi-Publish/` |
| macOS | `~/Library/Application Support/Multi-Publish/` |
| Linux | `~/.config/Multi-Publish/` |

**Q: 如何备份数据？**

A: 备份应用数据目录下的 `database.sqlite` 文件和 `config/` 目录即可。

**Q: 更换电脑如何迁移数据？**

A: 将旧电脑上的应用数据目录整个复制到新电脑的对应位置，覆盖后重启 App。

### 21.5 其他

**Q: 能否同时在多台电脑使用？**

A: 免费版限 1 台，Pro 版限 3 台。

**Q: 如何联系技术支持？**

A: 通过以下渠道：
- **GitHub Issues**：https://github.com/Colinchiu007/Multi-Publish/issues
- **Pro 用户**：享有优先级支持（48 小时内回复）

**Q: 如何提交功能建议？**

A: 在 GitHub Issues 提交 Feature Request，或通过设置页的「反馈」入口提交。

**Q: 是否会收集用户数据？**

A: 仅收集使用统计数据（如发布次数、功能使用率），用于改善产品体验。**不会收集**内容数据、账号密码、Cookie 等敏感信息。

---

## 三十一、更新日志

### v2.1.0 - 2026-07-02

**新增：**
- 发布间隔控制（PublishIntervalGuard）— 防止平台限流
- 共享数据分析服务（AnalyticsService）— Provider 模式注册
- 分块上传器（ChunkedUploader）— 大文件分片上传
- 代理池（ProxyPool）— 多代理自动轮换
- Instagram / Facebook 平台支持
- macOS DMG/ZIP 首次支持
- Linux AppImage 修复

### v2.0.1 - 2026-07-08

**新增：**
- Pro 许可证系统（LicenseManager）— 免费/试用/Pro
- 7 天免费试用机制
- 内容模板系统（TemplateManager）
- 升级弹窗（UpgradeModal）
- 试用心跳横幅（TrialBanner）

### v2.0.0 - 2026-07-02

**新增：**
- 首次运行引导 v2 — 4 步 Onboarding Wizard
- 发布失败告警弹窗 — 失败原因 + 重试操作
- i18n 国际化 — 中英文双语切换
- 离线模式（OfflineManager）
- Markdown 输入 + GFM 格式转换
- 封面模板生成器（Cover Generator）
- 全新设计体系（奶油·薰衣草）
- 移除 ElementPlus，自研 7 个基础组件

### v1.8.0 - 2026-07-03 — 插件系统
### v1.7.0 - 2026-07-03 — API 开放平台
### v1.6.0 - 2026-07-03 — 设计体系大重构
### v1.5.0 - 2026-06-28 — 云端发布
### v1.4.0 - 2026-06-28 — 内容情报引擎
### v1.3.0 - 2026-06-27 — Instagram/Facebook + 多账号同时发
### v1.2.0 - 智能发布 + 平台分类 + RpaViewManager 统一引擎
### v1.1.0 - 百家号 + 扫码登录 3 策略
### v1.0.0 - 2026-06-03 — 首个正式版本（10 平台）

---

> **文档版本**：v2.0 | **最后更新**：2026-07-08
> **适用版本**：v2.1.0+
>
> 本文档由产品团队维护，如有疑问或建议请联系。
>
> [GitHub 仓库](https://github.com/Colinchiu007/Multi-Publish) | [Issue 反馈](https://github.com/Colinchiu007/Multi-Publish/issues) | [Gitee 镜像](https://gitee.com/ColinChiu007/Multi-Publish)

