# 热门选题分类供给增强（方案 A-E 全量实现）

## 背景
财经/科技/情感/教育/健康/国际分类内容稀少。根因：抓取量不足 + society 黑洞词（裸字判）+ 单标签互斥 + 微博原生分类未映射 + 无垂类供给。

## 变更
- **A** MAX_PER_CHANNEL 20→50、MAX_TOPICS 160→400；分类器打分制多标签 categories[]（≤3）；微博映射扩容 + GENERIC_RAW_MAP；society 黑洞词修复
- **B/D** CATEGORY_BOOSTS 定向补拉：低于阈值或 UI 显式 boostCategories 触发（百度财经tab/新浪财经roll/IT之家RSS/微博情感·健康垂类）；独立限流熔断；boost id 含 board 段
- **C** LLM 分类兜底：general 条目批量分类（≤40/轮）+ hot_topics_llm_labels 落盘缓存（≤500）；fail-open 静默降级
- **E** UI：分类 chip 计数、双标签展示/过滤、空分类「补拉该分类」按钮；locale zh/en 成对（8 新键）

## 评审修复（CodeReview 无 CRITICAL；2 MAJOR 已修）
- boost 聚合移到 preservedStaleCache 早退之前（显式补拉不被主渠道限流吞掉）+ 回归测试 2 例
- boost 源 channelStats 连字符键 i18n 补齐；inFlight 显式 boost 不复用；categoryEmpty 排除渠道过滤误判；死导入清理

## 验证
- TDD 先红（15 契约）后绿：vitest 100/100（service 52 + HotTopics.vue 35 + assembly + phase1-context 13）
- check-locale-sync --keys PASS；eslint --quiet 0；pre-commit 质量节拍通过
- 详细规格：01-docs/PRD-HOT-TOPICS-CATEGORY-SUPPLY-2026-09-20.md

## 已知边界
- 教育/国际无稳定垂类端点（探测记录见 PRD §2），依赖 A+C 兜底
- 本地未跑打包/实时视觉基线，由 CI 覆盖（QM-1）
